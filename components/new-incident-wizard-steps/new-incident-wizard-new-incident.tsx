"use client"

export default function NewIncidentWizardStepNewIncident({ previousStep, nextStep }: { previousStep: () => void; nextStep: () => void }) {
    return (
        <div>
            Step1
        </div>
    );
}