"use client"

export default function NewIncidentWizardStepParticipants({ previousStep, nextStep }: { previousStep: () => void; nextStep: () => void }) {
    return (
        <div>
            Step2
        </div>
    );
}