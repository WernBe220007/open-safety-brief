services:
  db:
    image: "postgres:17"
    environment:
      POSTGRES_USER: "osb_user"
      POSTGRES_PASSWORD: "osb_password"
      POSTGRES_DB: "osb_db"
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
